# Auth Service
AUTH_SECRET_KEY=your-secret-key-here
AUTH_ALGORITHM=HS256
AUTH_ACCESS_TOKEN_EXPIRE_MINUTES=30
AUTH_REFRESH_TOKEN_EXPIRE_DAYS=7

# Database URLs
AUTH_DATABASE_URL=postgresql://swap_user:swap_password@postgres:5432/auth
USER_DATABASE_URL=postgresql://swap_user:swap_password@postgres:5432/users
LISTING_DATABASE_URL=postgresql://swap_user:swap_password@postgres:5432/listings
OFFER_DATABASE_URL=postgresql://swap_user:swap_password@postgres:5432/offers
PAYMENT_DATABASE_URL=postgresql://swap_user:swap_password@postgres:5432/payments
NOTIFICATION_DATABASE_URL=postgresql://swap_user:swap_password@postgres:5432/notifications
CHAT_DATABASE_URL=postgresql://swap_user:swap_password@postgres:5432/chat
ADMIN_DATABASE_URL=postgresql://swap_user:swap_password@postgres:5432/admin

# Redis
REDIS_URL=redis://redis:6379/0

# Kafka
KAFKA_BOOTSTRAP_SERVERS=kafka:29092

# MinIO
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
MINIO_URL=minio:9000
MINIO_SECURE=false

# SMTP (MailHog for development)
SMTP_HOST=mailhog
SMTP_PORT=1025
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=noreply@swap.local

# Services Ports
AUTH_SERVICE_PORT=8000
USER_SERVICE_PORT=8001
LISTING_SERVICE_PORT=8002
OFFER_SERVICE_PORT=8003
PAYMENT_SERVICE_PORT=8004
NOTIFICATION_SERVICE_PORT=8005
CHAT_SERVICE_PORT=8006
ADMIN_SERVICE_PORT=8007